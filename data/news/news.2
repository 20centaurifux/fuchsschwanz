# 2019-06-01

     Version 0.1.1 of Fuchsschwanz released today. It comes with the
     following new commands:
          * /echoback
          * /news
          * /v
          * /hush
          * /notify
          * /exclude
          * /wall
          * /log

     The following features were implemented:
          * /w command arguments (-s, -g, . and group name)
          * idle-boot and idle-mod options
