# 2019-09-?? v0.1.4

     Fuchsschwanz version 0.1.4 is now available! It comes with
     the following great enhancements:

          * FEATURE: email addresses are now hidden by default and
                     can be confirmed

                          -- see /m server ? confirmation

          * FEATURE: forward messages from the personal inbox to
                     your email address

                          -- see /m server ? /forward

          * FEATURE: request a new password by email

                          -- see /m server /newpasswd

          * FEATURE: admins can change the password of other users

                          -- see /m server /passwd
