# 2019-10-18 v0.1.5

     I'm happy to announce the release of Fuchsschwanz version 0.1.5!
     It comes with the following new features:

          * FEATURE: new avatar field for registered users

                          -- see /m server ? /avatar
                          -- see /m server ? /picture

          * FEATURE: admin commands for restarting/shutting down the
	             server

                          -- see /m server ? /restart
                          -- see /m server ? /shutdown
                          -- see /m server ? /abort

          * FEATURE: ip filter for blocking connections

                          -- see /m server /ipfilter

          * FEATURE: /whoami command
